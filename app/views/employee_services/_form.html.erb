<%= form_for(@employee_service) do |f| %>
  <% if @employee_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee_service.errors.count, "error") %> prohibited this employee_service from being saved:</h2>

      <ul>
      <% @employee_service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :employee_id %><br>
    <%= f.number_field :employee_id %>
  </div>
  <div class="field">
    <%= f.label :service_id %><br>
    <%= f.number_field :service_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
